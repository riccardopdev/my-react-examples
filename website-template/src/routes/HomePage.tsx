const HomePage = () => {
  return (
    <main>
      <section>
        <h1>Website Example</h1>
        <p>
          This is a typical website featuring a navigation and few pages with
          different content.
        </p>
        <p>Each page loads its own content from the corresponding endpoint.</p>
      </section>
    </main>
  );
};

export default HomePage;
